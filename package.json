{
  "name": "smartsummary",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && node index.js",
    "build": "cd server && npm install"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "stripe": "^14.0.0"
  }
}
